// react
import { useState, useEffect } from "react";

// mui
import { Box } from "@mui/material";

// plotly
import Plotly from "../components/plotly/Plotly.js";

export default function DemoApiPage() {
  return (
    <div>... will develope after DNS resolves</div>
  )
};
